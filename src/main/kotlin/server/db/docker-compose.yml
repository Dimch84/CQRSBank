version: "3.3"
services:
  postgres:
    container_name: cqrsServer
    image: postgres
    environment:
      POSTGRES_DB: "cqrs"
      POSTGRES_USER: "cqrs"
      POSTGRES_PASSWORD: "cqrs"
    volumes:
      - .:/docker-entrypoint-initdb.d
    ports:
      - "5433:5433"   # not work yet
